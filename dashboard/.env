# API base URL. Empty = same-origin: the unified ASP.NET Core app serves the dashboard and the
# API from one origin, so the frontend calls relative paths (/api/...). Leave this empty.
# For optional standalone frontend dev (`npm run dev`), vite.config proxies /api to :5200.
VITE_API_URL=
