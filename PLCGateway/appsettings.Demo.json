{
  "//": "Expo / offline demo profile. Run with:  dotnet run --project PLCGateway --environment Demo",
  "//_demo": "Demo:Enabled skips the PLC scan loop and the startup disconnect handler, so the",
  "//_demo2": "pre-seeded dataset is served untouched. NEVER deploy this file to a real gateway.",
  "Demo": {
    "Enabled": true,

    "//_sim": "EXPO SIMULATOR. Drives a synthetic machine in real time on top of the seeded",
    "//_sim2": "history: RunMinutes running (status Running, impellers fluctuating between AmpsMin",
    "//_sim3": "and AmpsMax), then IdleMinutes idle (status Idle, 0 A). Production, energy, cycle",
    "//_sim4": "count and blast time step up TOGETHER at the end of each run, never during it.",
    "//_sim5": "Setting this true also disables CycleTrackingService - the simulator writes",
    "//_sim6": "plc_cycles itself with the exact figures below.",
    "Simulator": {
      "Enabled": true,
      "RunMinutes": 5,
      "IdleMinutes": 2,
      "AmpsMin": 19.0,
      "AmpsMax": 20.0,
      "ProductionKgPerCycle": 300,
      "EnergyKwhPerCycle": 3.5,
      "ItemName": "Brake Drum"
    }
  },

  "//_imp": "How many impellers this machine has. Drives the amps panel, the spare-health grid",
  "//_imp2": "and the Section 2 per-impeller split. Production default is 10; the expo rig has 2.",
  "Impellers": {
    "Count": 2
  },

  "//_conn": "BOTH keys must point at the demo database: DatabaseService reads PostgreSQL:ConnectionString,",
  "//_conn2": "every API service reads ConnectionStrings:PostgresDb. Setting only one silently splits the app",
  "//_conn3": "across two databases.",
  "PostgreSQL": {
    "ConnectionString": "Host=localhost;Port=5432;Database=sreesakthi_gateway_demo;Username=postgres;Password=Pass"
  },
  "ConnectionStrings": {
    "PostgresDb": "Host=localhost;Port=5432;Database=sreesakthi_gateway_demo;Username=postgres;Password=Pass"
  }
}
